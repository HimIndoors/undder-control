<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="UndderControl.Views.TermsPage">
    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness"
            iOS="21"
            Android="21"
            WinPhone="21" />
    </ContentPage.Padding>
    <StackLayout>
        <ScrollView VerticalOptions="StartAndExpand">
            <ScrollView.Padding>
                <OnPlatform x:TypeArguments="Thickness"
                iOS="8"
                Android="8"
                WinPhone="8" />
            </ScrollView.Padding>
            <StackLayout>
                <Label x:Name="TermsTitle" FontSize="Subtitle" FontAttributes="Bold" />
                <Label x:Name="Terms" />
            </StackLayout>
        </ScrollView>
        <StackLayout VerticalOptions="End" Margin="0,21,0,0">
            <Button Text="Accept"  Command="{Binding OnAcceptCommand}" />
            <Button Text="Decline" Command="{Binding OnDeclineCommand}" />
        </StackLayout>
    </StackLayout>
  
</ContentPage>